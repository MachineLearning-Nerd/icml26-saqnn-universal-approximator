# Claim 4 limitations

Dense-unitary verification stops at six qubits because matrix size is
exponential; the CNOT recurrence itself is exact for arbitrary integer
qubit count. The controlled-`Rz` contribution is checked in the Claim 2 depth
audit rather than as a second standalone dense-unitary decomposition.
