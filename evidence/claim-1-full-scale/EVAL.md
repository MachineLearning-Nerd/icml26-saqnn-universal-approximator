# Claim 1 evaluation

Verdict: **VERIFIED**

The exact `[0,1]`-valued Theorem 1 contract is source-pinned and reproduced
with multivariate finite constructions, six convergence studies, a separate
independent implementation, and three sensitive negative controls. The formal
cumulative release-candidate run exited zero and also reran every previously
full-credit Claim 2–5 check.

The earlier signed `sin(x)` argument is not counted as evidence against this
claim because it violates the target-range assumption. It remains preserved
on the historical page as provenance; this additive evaluation corrects its
use for the live judge claim.
