[project]
name = "saqnn-reproduction"
version = "0.1.0"
description = "Pinned CPU environment for the SAQNN claim-by-claim reproduction"
requires-python = ">=3.12,<3.13"
dependencies = [
  "marimo>=0.15,<1",
  "matplotlib>=3.10,<4",
  "numpy>=2.2,<3",
]

[tool.uv]
package = false
